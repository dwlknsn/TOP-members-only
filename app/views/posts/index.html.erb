<h1>Posts#index</h1>
<p>Find me in app/views/posts/index.html.erb</p>

<ul>
  <% @posts.each do |post| %>
    <li>
      <%= post.title %><%= " by #{post.user.email}" if user_signed_in? %>
    </li>
  <% end %>
</ul>

<% if user_signed_in? %>
  <%= link_to "Create Post", new_post_path %>
<% end %>
